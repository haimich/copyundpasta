import EmojiUtil from "@/utils/EmojiUtil";

describe("getEmojisByCategory", () => {
  test("should return all emojis with categories", () => {
    let emojis = EmojiUtil.getEmojisByCategory();

    // console.log(emojis);
  });
});
